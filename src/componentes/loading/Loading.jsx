import React from 'react'
const Loading = () => {
          return (
                    <img src="src/img/Loading.gif" alt="loading" style={{
                              display: "flex", alignItems: "center"
                    }
                    } />

          )
}

export default Loading
